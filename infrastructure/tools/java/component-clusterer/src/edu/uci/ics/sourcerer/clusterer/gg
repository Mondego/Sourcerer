1) Most popular JDK packages
  Ordered list of java.* javax.* packages
2) Most popular classes (by method calls, instantiations)
3) 

1) Most popular non-JDK packages
  If starts with org, com or edu, 3rd level simple name is likely the project name. 
2) For each identified project, look at pattern of usage
   Also, try to get full list of methods for the project
   
Look at import statements and compare with usage listing from methods

Look at clusters of usage specific to different projects